import { FlexContainer, FormContainer } from "../ui/component.styled";
import { RegistrationContainer } from "../ui/container.styled";

function Registration() {
  return (
    <RegistrationContainer>
      <FlexContainer>
        <FormContainer>
          <h3>Registration Form</h3>
        </FormContainer>
      </FlexContainer>
    </RegistrationContainer>
  );
}

export default Registration;
